# Секреты — НЕ коммитить! Только для локальной сборки / CI.
# В коде ключи не хранить. Передавать через --dart-define при сборке.

# OneSignal (приложение + отправка пушей из приложения)
# flutter run --dart-define=ONESIGNAL_APP_ID=... --dart-define=ONESIGNAL_REST_API_KEY=...
ONESIGNAL_APP_ID=
ONESIGNAL_REST_API_KEY=

# Ключ для AI/чат (Google Gemini и т.п.) — только на бекенде (api.psylergy.com), в приложение не подставлять
# CHAT_AI_API_KEY=
