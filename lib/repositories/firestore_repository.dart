// lib/repositories/firestore_repository.dart

/// ВРЕМЕННАЯ ЗАГЛУШКА
/// Этот класс нужен только для того, чтобы приложение компилировалось
/// во время миграции на ApiRepository.
/// После полного перехода этот файл нужно будет снова удалить.
class FirestoreRepository {
  // Пустой конструктор
  FirestoreRepository();

// Можно добавить сюда пустые методы, на которые будет ругаться компилятор,
// чтобы он не останавливался на этом. Например:

// Future<void> someOldMethod() async {
//   logger.d("WARNING: Вызван устаревший метод someOldMethod из FirestoreRepository!");
// }
}